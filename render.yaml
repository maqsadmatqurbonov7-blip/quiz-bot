services:
  - type: worker
    name: quiz-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python quiz_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
